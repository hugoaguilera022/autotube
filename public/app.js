const $ = (s) => document.querySelector(s);
let currentVideoPlan = null;
const views = [...document.querySelectorAll('.view')];
const pageTitles={dashboard:'Tu contenido, automatizado.',create:'Crear un vídeo con IA.',production:'Producción del vídeo',projects:'Tus proyectos.',automation:'Automatiza tu canal.',connections:'Conexiones & API'};
function go(name){views.forEach(v=>v.classList.toggle('active-view',v.id===name));document.querySelectorAll('.nav').forEach(n=>n.classList.toggle('active',n.dataset.view===name));const title=$('#pageTitle');if(title)title.textContent=pageTitles[name]||'AutoTube';}
// Navegación robusta: delegación de eventos para que las secciones sigan funcionando aunque otro módulo registre handlers.
document.addEventListener('click',e=>{const b=e.target.closest('.nav,[data-go]');if(!b)return;const name=b.dataset.view||b.dataset.go;if(name){e.preventDefault();go(name);}});
window.go=go;
async function health(){try{const r=await fetch('/api/health');const d=await r.json();$('#serverStatus').textContent='Servidor conectado';$('#openaiBadge').textContent=d.configured.gemini?'Conectado':'Revisar'}catch{$('#serverStatus').textContent='Servidor no disponible'}}health();
function connectYoutube(){window.location.href='/api/youtube/auth';}
$('#youtubeBtn').onclick=connectYoutube;$('#youtubeConnect2').onclick=connectYoutube;$('#youtubeConnected').onclick=()=>go('connections');
function updateYoutubeGlobalStatus(connected, profile={}) {const connectButton=$('#youtubeBtn');const connectedButton=$('#youtubeConnected');if(!connectButton||!connectedButton)return;if(connected){connectButton.classList.add('hidden');connectedButton.classList.remove('hidden');$('#youtubeStatusTitle').textContent=profile.title||'YouTube conectado';$('#youtubeStatusAvatar').src=profile.avatar||'';}else{connectButton.classList.remove('hidden');connectedButton.classList.add('hidden');}}
async function loadYoutubeProfile(){try{const r=await fetch('/api/youtube/profile');const d=await r.json();const card=$('#youtubeProfile');if(!r.ok||!d.connected){card.classList.add('hidden');updateYoutubeGlobalStatus(false);return;}card.classList.remove('hidden');updateYoutubeGlobalStatus(true,d);$('#ytAvatar').src=d.avatar||'';$('#ytTitle').textContent=d.title||'Canal de YouTube';$('#ytHandle').textContent=d.handle?d.handle:'';$('#ytSubscribers').textContent=Number(d.subscribers||0).toLocaleString('es-ES');$('#ytVideos').textContent=Number(d.videos||0).toLocaleString('es-ES');$('#ytViews').textContent=Number(d.views||0).toLocaleString('es-ES');$('#ytChannelId').textContent=d.channelId||'—';}catch{$('#youtubeProfile').classList.add('hidden');updateYoutubeGlobalStatus(false);}}
$('#youtubeRefresh').onclick=loadYoutubeProfile;$('#youtubeDisconnect').onclick=async()=>{if(!confirm('¿Desconectar este canal de YouTube?'))return;await fetch('/api/youtube/disconnect',{method:'POST'});loadYoutubeProfile();};window.addEventListener('message',e=>{if(e.data?.type==='youtube_connected'){alert('YouTube conectado correctamente.');loadYoutubeProfile();}});loadYoutubeProfile();
async function apiJson(url,options={},label='solicitud'){let r;try{r=await fetch(url,options)}catch(e){throw new Error('No se pudo conectar con '+label+'.')}const raw=await r.text();let d=null;try{d=raw?JSON.parse(raw):null}catch(e){throw new Error('Respuesta no válida del servidor ('+r.status+') en '+url+'.')}if(!r.ok)throw new Error(d?.error||'Error del servidor ('+r.status+').');return d;}
function validateYoutubeReference(value){const raw=String(value||'').trim();if(!raw)return '';try{const u=new URL(raw),host=u.hostname.toLowerCase(),youtube=host==='youtu.be'||host==='youtube.com'||host==='www.youtube.com'||host.endsWith('.youtube.com');if(!youtube)throw new Error('La URL no pertenece a YouTube.');const watch=host.endsWith('youtube.com')&&u.pathname==='/watch'&&u.searchParams.get('v'),shorts=host.endsWith('youtube.com')&&u.pathname.startsWith('/shorts/'),embed=host.endsWith('youtube.com')&&u.pathname.startsWith('/embed/'),short=host==='youtu.be'&&u.pathname.length>1;if(!(watch||shorts||embed||short))throw new Error('Introduce una URL de vídeo de YouTube válida.');return u.href;}catch(e){if(e?.message&&!/Invalid URL/i.test(e.message))throw e;throw new Error('La URL de referencia de YouTube no es válida.');}}
async function analyzeUploadedReference(file){if(!file)return null;const form=new FormData();form.append('video',file);const d=await apiJson('/api/reference/visual-analysis',{method:'POST',body:form},'el análisis visual');return d?.analysis||null;}
async function analyzeReferenceForCreation(reference){
  const value=validateYoutubeReference(reference);
  if(!value)return null;
  const started=await apiJson('/api/youtube/reference',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reference:value})},'el inicio del análisis de YouTube');
  if(started?.status==='done')return started;
  const jobId=started?.jobId;
  if(!jobId)throw new Error('El servidor no devolvió el identificador del análisis de YouTube.');
  const statusUrl=started?.statusUrl||('/api/youtube/reference/'+encodeURIComponent(jobId));
  for(let i=0;i<180;i++){
    await new Promise(r=>setTimeout(r,2000));
    const status=await apiJson(statusUrl,{},'el estado del análisis de YouTube');
    if(status?.status==='done')return status;
    if(status?.status==='error')throw new Error(status.error||'No se pudo analizar el vídeo completo de YouTube.');
  }
  throw new Error('El análisis de YouTube está tardando más de lo esperado. Puedes volver a intentarlo sin perder la estructura.');
}
async function generateUrlToVideo(){
  const reference=String($('#reference').value||'').trim();
  if(!reference)return alert('Añade primero una URL de YouTube.');
  let value;try{value=validateYoutubeReference(reference)}catch(e){return alert(e.message)}
  const btn=$('#urlToVideoBtn'),state=$('#generationState'),preview=$('#preview');
  btn.disabled=true;btn.textContent='Descargando MP4…';state.textContent='Descargando vídeo original…';
  preview.innerHTML='<div class="empty"><div class="empty-icon">◉</div><b>Descargando el vídeo original…</b><p>AutoTube conserva el vídeo y el audio originales. Solo remuxa el contenedor cuando es necesario para obtener MP4.</p></div>';
  try{
    const started=await apiJson('/api/url-to-mp4',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reference:value})},'la descarga del vídeo original');
    const jobId=started?.jobId;if(!jobId)throw new Error('El servidor no devolvió el identificador del trabajo.');
    let finished=null;
    for(let i=0;i<600;i++){
      await new Promise(r=>setTimeout(r,2000));
      const status=await apiJson('/api/url-to-mp4/'+encodeURIComponent(jobId),{},'el estado del MP4');
      if(status.status==='done'){finished=status;break;}
      if(status.status==='error')throw new Error(status.error||'No se pudo generar el MP4.');
      const p=Math.max(0,Math.min(99,Number(status.progress)||0));
      state.textContent=p?'Generando MP4 '+p+'%':'Descargando y validando…';
      btn.textContent=p?'MP4 '+p+'%':'Descargando MP4…';
    }
    if(!finished)throw new Error('El proceso está tardando más de lo esperado.');
    const rr=await fetch(finished.downloadUrl);if(!rr.ok)throw new Error('El MP4 terminó pero no se pudo descargar.');
    const blob=await rr.blob();if(blob.size<1000)throw new Error('El MP4 descargado está vacío o incompleto.');
    const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='autotube-original.mp4';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),60000);
    const s=finished.source||{},f=finished.final||{};
    state.textContent='MP4 listo y validado';
    preview.innerHTML='<div class="empty"><div class="empty-icon">✓</div><b>MP4 original listo</b><p>Vídeo y audio conservados. '+String(f.width||s.width||'')+'×'+String(f.height||s.height||'')+' · '+String(f.fps||s.fps||'')+' fps · '+String(f.duration||s.duration||'')+' s · modo '+String(finished.mode||'copy')+'.</p></div>';
  }catch(e){
    state.textContent='Error';preview.innerHTML='<div class="empty"><div class="empty-icon">!</div><b>No se pudo generar el MP4</b><p>'+escapeHtml(e.message)+'</p></div>';
  }finally{btn.disabled=false;btn.textContent='▶ Generar MP4 exacto desde esta URL';}
}
function openProduction(plan){currentVideoPlan={...currentVideoPlan,...plan};$('#productionTitle').textContent=plan.title||'Nuevo vídeo';const metaDuration=currentVideoPlan?.referenceDurationSeconds?Math.ceil(Number(currentVideoPlan.referenceDurationSeconds)/60):(plan.duration||$('#duration').value);$('#productionMeta').textContent=(plan.outline?.length||0)+' bloques de contenido · '+metaDuration+' min · '+(plan.language||$('#language').value);$('#productionState').textContent='Listo para producir';$('#scenesList').innerHTML='<div class="card empty"><div class="empty-icon">🎬</div><b>Plan preparado</b><p>Pulsa “Generar escenas con IA” para crear el montaje escena por escena.</p></div>';go('production')}
$('#backToCreate').onclick=()=>go('create');$('#generateMusicBtn').onclick=generateMusic;$('#findMediaBtn').onclick=loadSceneMedia;$('#generateAiVideoBtn').onclick=generateAiSceneClips;$('#generateVoiceBtn').onclick=generateVoiceForScenes;$('#renderVideoBtn').onclick=renderFinalVideo;
$('#buildProductionBtn').onclick=async()=>{if(!currentVideoPlan?.topic)return alert('Primero genera la estructura del vídeo.');const btn=$('#buildProductionBtn'),progress=$('#productionProgress'),bar=$('#productionProgressBar'),value=$('#productionProgressValue'),label=$('#productionProgressLabel');btn.disabled=true;btn.textContent='Generando…';progress.classList.remove('hidden');$('#productionState').textContent='Produciendo';bar.style.width='12%';value.textContent='12%';label.textContent='Analizando estructura…';try{await new Promise(r=>setTimeout(r,350));bar.style.width='35%';value.textContent='35%';label.textContent='Diseñando escenas…';const d=await apiJson('/api/ai/production-plan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(currentVideoPlan)},'la generación de escenas');currentVideoPlan={...currentVideoPlan,...d};bar.style.width='78%';value.textContent='78%';label.textContent='Preparando narración y visuales…';renderScenes(d);await new Promise(r=>setTimeout(r,250));bar.style.width='100%';value.textContent='100%';label.textContent='Plan de producción listo';$('#productionState').textContent='Listo';}catch(e){$('#productionState').textContent='Error';label.textContent='Error';$('#scenesList').innerHTML='<div class="card empty"><div class="empty-icon">!</div><b>No se pudo generar</b><p>'+escapeHtml(e.message)+'</p></div>'}finally{btn.disabled=false;btn.textContent='Regenerar escenas con IA'}};
function renderScenes(d){const scenes=Array.isArray(d?.scenes)?d.scenes:[];const box=$('#scenesList');if(!box)return;if(!scenes.length){box.innerHTML='<div class="card empty"><div class="empty-icon">!</div><b>No se generaron escenas</b><p>Regenera el plan de producción para continuar.</p></div>';return}box.innerHTML='<div class="section-head"><h3>'+scenes.length+' escenas preparadas</h3><span>'+escapeHtml(d.musicMood||'Música pendiente')+' · '+escapeHtml(d.voiceStyle||'Voz pendiente')+'</span></div>'+scenes.map(s=>'<div class="card scene-card"><div class="scene-number">'+String(s.number).padStart(2,'0')+'</div><div class="scene-body"><div class="scene-title"><div><b>'+escapeHtml(s.title||'Escena')+'</b><span>'+escapeHtml(String(s.duration||0))+' s · '+escapeHtml(s.transition||'Transición suave')+'</span></div><span class="badge">VISUAL + VOZ</span></div><p><strong>Narración:</strong> '+escapeHtml(s.narration||'')+'</p><p class="visual-prompt"><strong>Visual:</strong> '+escapeHtml(s.visualPrompt||'')+'</p></div></div>').join('')}
async function generateMusic(){if(!currentVideoPlan?.topic)return alert('Primero genera el plan del vídeo.');const btn=$('#generateMusicBtn'),state=$('#musicState'),audio=$('#musicPlayer'),label=$('#musicLabel');btn.disabled=true;btn.textContent='Generando música…';state.textContent='Generando';try{const r=await fetch('/api/ai/music',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({topic:currentVideoPlan.topic,mood:currentVideoPlan.musicMood||'instrumental original',audioProfile:currentVideoPlan.referenceStyle?.visualAnalysis?.audioProfile||currentVideoPlan.referenceAudioProfile||{},durationSeconds:Math.min(300,Math.max(30,Number(currentVideoPlan.duration||8)*60))})});const d=await r.json().catch(()=>null);if(!r.ok||!d?.jobId)throw new Error(d?.error||'No se pudo iniciar la generación de música.');const jobId=d.jobId;let finished=null;for(let i=0;i<450;i++){await new Promise(resolve=>setTimeout(resolve,2000));const status=await apiJson('/api/ai/music/'+encodeURIComponent(jobId),{},'el estado de la música');if(status.status==='done'){finished=status;break;}if(status.status==='error')throw new Error(status.error||'No se pudo generar la música.');const p=Math.max(0,Math.min(99,Number(status.progress)||0));state.textContent=p>0?'Generando '+p+'%':'Generando';btn.textContent=p>0?'Música '+p+'%':'Generando música…';}if(!finished)throw new Error('La generación musical está tardando más de lo esperado.');const rr=await fetch(finished.downloadUrl);if(!rr.ok)throw new Error('La música terminó de generarse pero no se pudo descargar.');const blob=await rr.blob();if(window.currentMusicUrl)URL.revokeObjectURL(window.currentMusicUrl);window.currentMusicUrl=URL.createObjectURL(blob);currentVideoPlan.musicUrl=window.currentMusicUrl;audio.src=window.currentMusicUrl;audio.classList.remove('hidden');audio.load();label.textContent='Música original adaptada al perfil del referente · lista para el montaje';state.textContent='Listo';}catch(e){state.textContent='Error';label.textContent=e.message;}finally{btn.disabled=false;btn.textContent='Generar música gratuita'}}
async function generateVoiceForScenes(){const scenes=currentVideoPlan?.scenes||[];if(!scenes.length)return alert('Primero genera las escenas.');const btn=$('#generateVoiceBtn'),state=$('#voiceState'),audio=$('#voicePlayer');btn.disabled=true;btn.textContent='Generando voz…';state.textContent='Creando narración por escenas';try{const blobs=[];for(const scene of scenes){const r=await fetch('/api/ai/voice',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({
        text:scene.narration||scene.script||'',
        language:currentVideoPlan.language||'es',
        style:currentVideoPlan.voiceStyle||currentVideoPlan.referenceStyle?.visualAnalysis?.audioProfile?.voiceStyle||'Natural y cercana',
        audioProfile:currentVideoPlan.referenceStyle?.visualAnalysis?.audioProfile||currentVideoPlan.referenceAudioProfile||{}
      })});if(!r.ok){let d={};try{d=await r.json()}catch{}throw new Error(d.error||'No se pudo generar una voz.');}blobs.push(await r.blob());}currentVideoPlan.narrationAudio=blobs.map(b=>URL.createObjectURL(b));if(blobs[0]){audio.src=currentVideoPlan.narrationAudio[0];audio.classList.remove('hidden');audio.load();}state.textContent='Voz lista';}catch(e){state.textContent='Error';alert(e.message)}finally{btn.disabled=false;btn.textContent='Generar narración IA'}}
async function renderFinalVideo(){const scenes=currentVideoPlan?.scenes||[],mediaResults=currentVideoPlan?.mediaResults||[],aiClips=currentVideoPlan?.aiClips||[];if(!scenes.length||(!mediaResults.length&&!aiClips.length))return alert('Primero genera los vídeos IA de las escenas o busca visuales de respaldo.');const btn=$('#renderVideoBtn'),state=$('#renderState');btn.disabled=true;btn.textContent='Renderizando…';state.textContent=aiClips.length?'Preparando vídeos IA…':'Preparando clips…';try{const form=new FormData();form.append('scenes',JSON.stringify(scenes));form.append('mediaResults',JSON.stringify(mediaResults));for(const clip of aiClips){form.append('aiClips',clip,clip.name||'ai-scene.mp4')}form.append('language',currentVideoPlan?.language||'es');for(const url of(currentVideoPlan?.narrationAudio||[]).filter(Boolean)){const rr=await fetch(url);if(!rr.ok)throw new Error('No se pudo preparar una narración para el montaje.');form.append('narration',await rr.blob(),'narration.wav')}if(currentVideoPlan?.musicUrl){const rr=await fetch(currentVideoPlan.musicUrl);if(!rr.ok)throw new Error('No se pudo preparar la música para el montaje.');form.append('music',await rr.blob(),'music.wav')}const r=await fetch('/api/render',{method:'POST',body:form});const d=await r.json().catch(()=>null);if(!r.ok)throw new Error(d?.error||'No se pudo iniciar el render.');const jobId=d?.jobId;if(!jobId)throw new Error('El servidor no devolvió el identificador del render.');let finished=null;for(let i=0;i<450;i++){await new Promise(resolve=>setTimeout(resolve,2000));const status=await apiJson('/api/render/'+encodeURIComponent(jobId),{},'el estado del render');if(status.status==='done'){finished=status;break;}if(status.status==='error')throw new Error(status.error||'No se pudo renderizar el vídeo.');const p=Math.max(0,Math.min(99,Number(status.progress)||0));state.textContent=p>0?'Renderizando… '+p+'%':'Renderizando vídeo…';btn.textContent=p>0?'Renderizando '+p+'%':'Renderizando…';}if(!finished)throw new Error('El render está tardando más de lo esperado. Puedes volver a consultar el estado.');state.textContent='Preparando descarga…';const videoResponse=await fetch(finished.downloadUrl);if(!videoResponse.ok)throw new Error('El MP4 terminó de renderizarse, pero no se pudo descargar.');const blob=await videoResponse.blob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='autotube-final.mp4';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),60000);state.textContent='MP4 listo';}catch(e){state.textContent='Error';alert(e.message)}finally{btn.disabled=false;btn.textContent='🎬 Renderizar MP4'}}
async function generateAiSceneClips(){const scenes=currentVideoPlan?.scenes||[];if(!scenes.length)return alert('Primero genera las escenas.');const btn=$('#generateAiVideoBtn'),state=$('#mediaState');btn.disabled=true;btn.textContent='Generando vídeos IA…';state.textContent='Conectando con Hugging Face ZeroGPU optimizado';try{const mod=await import('https://cdn.jsdelivr.net/npm/@gradio/client/dist/index.min.js');const Client=mod.Client;const space='DeepRat/LTX-Video-ZeroGPU-Optimized';const client=await Client.connect(space);const clipCount=Math.min(8,Math.max(1,scenes.length));const clips=[];for(let i=0;i<clipCount;i++){const scene=scenes[Math.min(scenes.length-1,Math.round(i*(scenes.length-1)/Math.max(1,clipCount-1)))];state.textContent='Generando vídeo IA '+(i+1)+' de '+clipCount+' (cuota optimizada)…';const refAnalysis=currentVideoPlan?.referenceStyle?.visualAnalysis||currentVideoPlan?.visualReferenceAnalysis||{},vp=refAnalysis?.videoProfile||{},ap=refAnalysis?.animationProfile||{},sp=refAnalysis?.structureProfile||{},gd=refAnalysis?.generationDirectives||{};const audiovisualDNA=[vp.visualStyle&&('visual style: '+vp.visualStyle),vp.composition&&('composition/framing: '+vp.composition),vp.palette&&('palette: '+vp.palette),vp.lighting&&('lighting: '+vp.lighting),vp.cameraMovement&&('camera movement: '+vp.cameraMovement),vp.continuity&&('continuity: '+vp.continuity),ap.cameraMotion&&('camera animation: '+ap.cameraMotion),ap.zoomStyle&&('zoom: '+ap.zoomStyle),ap.panStyle&&('pan: '+ap.panStyle),ap.overlays&&('overlays/graphics: '+ap.overlays),ap.effects&&('effects: '+ap.effects),ap.transitionStyle&&('transitions: '+ap.transitionStyle),ap.motionIntensity&&('motion intensity: '+ap.motionIntensity),ap.visualRhythm&&('visual rhythm: '+ap.visualRhythm),sp.pacing&&('editing pacing: '+sp.pacing),sp.visualContinuity&&('visual continuity: '+sp.visualContinuity),gd.preserveVisualContinuity&&'preserve visual continuity between scenes'].filter(Boolean).join('; ');const previousScene=scenes[i-1],nextScene=scenes[i+1];const segment=scene.referenceSegment||refAnalysis?.structureProfile?.sceneSegments?.[Math.min((refAnalysis?.structureProfile?.sceneSegments?.length||1)-1,Math.round(i*((refAnalysis?.structureProfile?.sceneSegments?.length||1)-1)/Math.max(1,scenes.length-1)))]||null;const segmentDNA=segment?[segment.startSeconds!=null&&('reference time range: '+segment.startSeconds+'s-'+segment.endSeconds+'s'),segment.summary&&('reference segment summary: '+segment.summary),segment.subject&&('reference segment subject: '+segment.subject),segment.shotScale&&('reference shot scale: '+segment.shotScale),segment.composition&&('reference segment composition: '+segment.composition),segment.cameraMovement&&('reference segment camera movement: '+segment.cameraMovement),segment.motionIntensity&&('reference segment motion intensity: '+segment.motionIntensity),segment.lighting&&('reference segment lighting: '+segment.lighting),segment.palette&&('reference segment palette: '+segment.palette),segment.transitionIn&&('reference transition in: '+segment.transitionIn),segment.transitionOut&&('reference transition out: '+segment.transitionOut),segment.audioRole&&('reference audio role: '+segment.audioRole),segment.narrationRole&&('reference narration role: '+segment.narrationRole),segment.continuityAnchor&&('reference continuity anchor: '+segment.continuityAnchor),segment.generationPrompt&&('reference segment generation direction: '+segment.generationPrompt)].filter(Boolean).join('; '):'';const continuity=[previousScene&&('previous scene ended with: '+String(previousScene.visualPrompt||previousScene.title||'').slice(0,300)),nextScene&&('next scene should lead naturally toward: '+String(nextScene.visualPrompt||nextScene.title||'').slice(0,300))].filter(Boolean).join('. ');const prompt=[scene.visualPrompt||scene.title||'Cinematic scene',scene.animationNotes&&('scene-specific animation and camera direction: '+scene.animationNotes),segmentDNA&&('TIMESTAMP-SPECIFIC REFERENCE SEGMENT: '+segmentDNA),audiovisualDNA&&('reference audiovisual characteristics to preserve: '+audiovisualDNA),continuity,'Generate ORIGINAL content only. Match the reference audiovisual language, shot scale, framing, camera behavior, motion intensity, lighting, palette, transitions and pacing without reproducing any identifiable shot, frame, character, text, logo, recording or copyrighted expression.','Realistic high-quality cinematography, 16:9 landscape, coherent natural motion, detailed textures, professional continuous camera movement, normal speed, no text, no logos, no watermark.'].filter(Boolean).join('. ');const r=await client.predict('text_to_video',[prompt,'worst quality, inconsistent motion, blurry, jittery, distorted, text, logos, watermark, slow motion',null,null,704,960,'text-to-video',2.0,9,Math.floor(Math.random()*4294967295),true,3,true,false]);const o=r?.data?.[0],u=typeof o==='string'?o:(o?.url||o?.path||o?.video?.url||'');if(!u)throw new Error('LTX no devolvió el vídeo de la escena '+(i+1)+'.');const rr=await fetch(u);if(!rr.ok)throw new Error('No se pudo descargar el vídeo IA de la escena '+(i+1)+' ('+rr.status+').');clips.push(new File([await rr.blob()],'ai-scene-'+(i+1)+'.mp4',{type:'video/mp4'}));}currentVideoPlan.aiClips=clips;state.textContent='✓ '+clips.length+' vídeos IA optimizados listos para el montaje';renderAiClipResults(clips);}catch(e){state.textContent='No se pudo generar vídeo IA';const msg=String(e?.message||e);if(/quota|zerogpu|exceeded/i.test(msg)){alert('La cuota gratuita de vídeo IA de Hugging Face no tiene tiempo suficiente ahora mismo. AutoTube ha detenido la generación sin continuar consumiendo cuota. No pulses Renderizar MP4. La cuota se restablece según el ciclo de Hugging Face.');}else{alert(msg);}}finally{btn.disabled=false;btn.textContent='🎥 Generar vídeos IA (cuota optimizada)'}}
function renderAiClipResults(clips){const box=$('#mediaResults');if(!box)return;box.innerHTML='<div class="card"><div class="section-head"><h3>Vídeos IA generados</h3><span>Fuente: LTX Video · ZeroGPU</span></div>'+clips.map((f,i)=>'<div style="display:flex;gap:12px;align-items:center;padding:8px 0;border-top:1px solid #252a33"><b>Escena '+(i+1)+'</b><span class="muted">'+Math.round(f.size/1024)+' KB · 960×704</span></div>').join('')+'</div>'}
async function loadSceneMedia(){const scenes=currentVideoPlan?.scenes||[];if(!scenes.length)return alert('Primero genera las escenas.');const btn=$('#findMediaBtn'),state=$('#mediaState');btn.disabled=true;btn.textContent='Buscando visuales…';state.textContent='Pexels + Pixabay';try{const d=await apiJson('/api/media/search',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({scenes,referenceTopic:currentVideoPlan?.referenceTopic||currentVideoPlan?.referenceData?.title||currentVideoPlan?.topic||''})},'la búsqueda de visuales');currentVideoPlan.mediaResults=d.results||[];renderMediaResults(d.results||[]);state.textContent='Listo';}catch(e){state.textContent='Error';alert(e.message)}finally{btn.disabled=false;btn.textContent='Buscar visuales de las escenas'}}
function renderMediaResults(results){const box=$('#mediaResults');if(!box)return;box.innerHTML=results.map(r=>'<div class="card media-scene"><div class="media-scene-head"><b>Escena '+escapeHtml(String(r.number))+': '+escapeHtml(r.title||'')+'</b><span>'+escapeHtml(r.query||'')+'</span></div><div class="media-grid">'+(r.media||[]).slice(0,6).map(m=>'<a class="media-item" href="'+escapeHtml(m.url||'#')+'" target="_blank" rel="noopener"><img src="'+escapeHtml(m.thumbnail||'')+'" alt=""><div><b>'+escapeHtml(m.provider)+'</b><span>'+escapeHtml(String(m.duration||0))+' s</span></div></a>').join('')}</div></div>').join('')||'<div class="card empty">No se encontraron visuales.</div>'}
function escapeHtml(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}

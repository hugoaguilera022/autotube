name: AutoTube syntax check
on:
  push:
    branches: [main]
    paths: ['server.js','.github/workflows/syntax-check.yml']
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check server.js
